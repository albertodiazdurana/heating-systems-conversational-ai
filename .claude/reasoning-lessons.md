# Reasoning Lessons — Heating Systems Conversational AI

Lessons distilled from session transcript analysis (STAA). Entries are tagged
by source session and scope (`ecosystem` | `pattern` | `project`).

## Verification & Assertions

- [STAA] S1 [ecosystem]: Project type detection from folder structure alone is unreliable when `_reference/` contains substantive scope material. Read `_reference/` BEFORE finalizing project type in `/dsm-go`.
- [STAA] S1 [ecosystem]: When CLAUDE.md or `_reference/` cites external paths, verify with `ls` before quoting them in research or plan documents.
- [STAA] S3 [ecosystem]: Before treating a third-party framework feature as missing, read the source, not just the docs. "Undocumented" ≠ "unimplemented."
- [STAA] S3 [pattern]: When preliminary research recommends a framework, verify the comparison baseline matches the actual alternatives in scope. Comparing to the wrong alternative is a silent failure mode that survives review.
- [auto] S4 [ecosystem]: `BaseTool.handle_tool_error` is narrower than its name implies, it only catches `ToolException` subclasses, not generic `Exception`. Read the source before treating an opt-in recovery flag as universal. Verified empirically in `tests/test_tool_error_handling.py` (BL-002 edit 4).
- [auto] S4 [pattern]: Frame "assumed behavior" tests as empirical probes with three pre-enumerated outcome categories (PASS / expected fail / unexpected fail). Each failure mode becomes a documented finding, not a blocker. One probe delivered two cascading findings in this session (default propagation + narrow `handle_tool_error` scope).
- [STAA] S4 [pattern]: Pre-enumerated outcome categories are a starting point, not a complete catalog. An unexpected N+1 outcome (e.g., `FakeMessagesListChatModel.bind_tools` NotImplementedError before the expected propagation/catch fork) is information, not a blocker. Document each as a finding and keep probing. Extends the `[auto] S4` empirical-probe lesson.
- [STAA] S5 [ecosystem]: "Find the root cause" from the user is a directive to triangulate (template spec + canonical instance + offending instance) BEFORE proposing a patch. Not "patch first, explain after." Observed when user paused Sprint 1 close to ask why the plan lacked a Sprint Boundary Checklist; the three-layer diagnosis (template exists → no audit tool → plan silently non-compliant) became the feedback artifact.

## Pipeline Discipline

- [STAA] S1 [ecosystem]: Material in `_reference/` is preliminary input, not actionable. Pipeline is `_reference/` → `dsm-docs/research/` → `dsm-docs/plans/` → implementation. Never propose implementation directly from `_reference/`.
- [STAA] S1 [ecosystem]: Defer edits to CLAUDE.md Section 4 (project specifics) until research validates assumptions. Section 4 is downstream of the planning pipeline, not upstream.
- [STAA] S3 [pattern]: For experimental projects, framework choice should optimize for replaceability of subsystems, not for stack uniformity. State the project's experimental vs. production posture before choosing a framework.
- [STAA] S4 [ecosystem]: Before `git mv <research-file> done/`, grep the repo for inbound references from open BLs, in-flight sprints, and decision records. If ANY active artifact cites it, HOLD until that artifact closes. Reversing a move disrupts git history and inbound links. Observed with 6 research files where only 1 was unambiguously consumable.

## Decision Heuristics

- [STAA] S1 [pattern]: When recommending tech-stack defaults that touch core dependencies (LLM provider, framework), present options with tradeoffs rather than a single recommendation. Defaults reveal training-data bias.
- [STAA] S3 [pattern]: When a pivot decision requires both "validate option X" and "validate combination of X+Y," split into named parallel research tracks; converge at the plan-update stage.
- [STAA] S3 [pattern]: Before executing a pivot batch, enumerate surviving artifacts explicitly ("sunk cost is X; Y and Z survive because they are framework-agnostic"). Converts vague rework fear into a scoped inventory and exposes any hidden coupling.
- [STAA] S4 [pattern]: Plan signature sketches are illustrative when the plan says "port from X"; the ported module's signature is authoritative unless the plan explicitly overrides with rationale. Verified on `src/tools/heating_curve.py`: plan's anchor-form `flow_temp(outside_temp, slope, offset, design_outside_temp, design_flow_temp)` was overridden by the companion app's `T_base + slope * (T_room - T_outdoor)` form.
- [STAA] S4 [pattern]: At Gate 2, when all remaining decision flags have trivial (≤5 LOC) implementation differences, collapse to a single recommended path with a one-line opt-out rather than presenting a 3-option palette. Palettes on trivial-LOC decisions create fatigue because the real trade-off is semantic, not implementation. Observed in step 7 config.py: user asked "is the implementation a lot different?" then "go with your recommendation."
- [STAA] S5 [ecosystem]: When a user paste is ambiguous between "approve" and "challenge" (e.g., re-pasting a FAIL output you just acknowledged), default to the challenge reading. Re-examine and present counter-options rather than accepting the acknowledgement as settled. Observed at Q2 smoke FAIL: user pasted fail output, safer read was "re-examine" → led to Option B fix + 5/5 pass instead of closing with a documented gap.
- [STAA] S5 [pattern]: For open-ended feasibility questions ("can we finish X this session?"), reply with preconditions, not yes/no. Convert "can we?" into "we can if {bandwidth ok, scope cut Y, hard-stop on Z}." Makes the cost contract explicit and gives user a concrete accept/reject rather than a vague commitment. Observed at 14:36 Sprint 1 in-session close decision.

## Batching & Efficiency

- [STAA] S1 [ecosystem]: When research surfaces multiple decision points, batch them into a numbered list for one user response rather than asking serially.
- [STAA] S3 [ecosystem]: When research output drives multiple downstream artifact changes, present them as a labeled batch (A/B/C…) for single-pass approval.
- [STAA] S4 [ecosystem]: When a parallel session refuses work due to overlapping uncommitted main-session changes, prefer a commit boundary on the nearest natural seam (governance/docs before implementation begins) over narrowing parallel scope (defers outputs) or moving the work to main (defeats parallelism). BL-001 unblock landed as three commits split on the align / docs / plan seam.
- [STAA] S5 [pattern]: For research matrices of N entities × M sources, partition subagents by source-type, not by entity. N-by-entity causes M redundant fetches per source; N-by-source lets each agent own its fetch set with zero overlap. Observed at 13:03 model-selection research: 3 subagents (vendor-facts / BFCL / multilingual-DE) covered 7 models without duplicate fetches.
- [STAA] S5 [pattern]: Bandwidth-bound multi-minute operations (package pulls, model downloads, long CI runs) are slots for staging the next step, not waiting. Pre-write the code/doc that the download will unblock so it's ready at completion. Observed at 15:30 Ollama pull: ~6-min wait used to draft `scripts/smoke_test.py`, which ran successfully on first invocation post-pull.

## Protocol Hygiene

- [STAA] S1 [ecosystem]: Session Transcript Protocol "read last 3 lines, anchor on last non-empty line" should be paired with a monotonic-timestamp check; backwards timestamps indicate protocol violation.
- [STAA] S1 [ecosystem]: When re-running or refining work already reported, append a delta block rather than re-emitting the original output block.
- [STAA] S3 [ecosystem]: Session Transcript Protocol violations are silent. The harness does not enforce appends; only a user reminder catches the lapse. Self-check at every output: "did I append a thinking block since the last user turn?" If no, append before output.
- [STAA] S3 [ecosystem]: `git mv` requires the source to be tracked. For files staged-but-not-committed or untracked, use plain `mv` then `git add` the new path.
- [auto] S4 [pattern]: When a deprecation warning surfaces on the FIRST pytest run of new code, halt and decide migration before committing. Shipping with the warning means the deprecated API spreads across modules and the later migration is a multi-file refactor instead of a two-line import swap.
- [STAA] S4 [ecosystem]: Persistent session markers (`session-baseline.txt`, `last-wrap-up.txt`, checkpoint filename) are authoritative for session numbering; inline transcript labels ("Session N+1 cont") may drift. At any session-number-sensitive moment (wrap-up, commits, PR titles, MEMORY.md updates), trust the persistent markers, not the transcript header.

## Cross-Repo & Governance

- [STAA] S3 [ecosystem]: Before initiating a cross-repo contribution (PR, issue, doc patch), check whether the target ecosystem has a contribution playbook. If not, request one via inbox before drafting.
- [S4] S4 [ecosystem]: Treat the capability experiment and the upstream contribution as ONE pipeline, not two tracks. The experiment's primary job is a pass/fail project decision; the docs/bug gaps surface as a side effect. The experiment script + a short deep-dive note are the evidence base for the contribution, no separate work needed. Follow issue-before-PR to gauge maintainer response; time-box the wait (~2 weeks for active projects). Source: graph-explorer FalkorDBLite playbook (2026-04-13 inbox).
- [auto] S5 [pattern]: Apply an explicit evidence-strength scale (third-party benchmark > vendor benchmark > vendor capability claim > anecdotal) when ranking candidate technologies. Treating "vendor claims X" as equal to "third-party benchmark scores model on X" distorts rankings toward easiest-to-find evidence. Caught a bad first ranking under user pushback; re-ranking on the scale put the most-evidenced candidate (llama3.1:8b: BFCL 76.1 + MMLU-DE 60.59) ahead of the convenient-fit candidate (qwen3:4b: zero measured numbers).
- [auto] S5 [pattern]: Constraints covered by a documented plan-level fallback (e.g., "OpenAI escape hatch if local fails") become *soft*, even when they look hard in isolation. Treating soft constraints as hard distorts ranking toward "cheap to satisfy." Pre-flight: for each criterion, is there a documented fallback? If yes, classify as soft.
- [auto] S5 [pattern]: At Pre-Generation Brief Gate 2, surface the strongest counter-evidence to your own recommendation BEFORE asking for approval. Procedural addition to the four-gate model. Caught two ranking errors this session via user pushback that should have been caught by the agent's own counter-evidence step.
- [auto] S5 [project]: Read §6 exit criteria boxes strictly, not pragmatically. When a smoke test fails one box (Q2 ValueError) but passes the gating box (Q5), the strict read is "NOT met." Closing on a partial-pass §6 sets a precedent of corner-cutting; the Sprint Boundary Checklist exists to catch this.
- [auto] S5 [pattern]: When multiple candidates have zero measured evidence on a criterion, tiebreakers MUST favor cleanly-dominant alternatives. "Documented-but-unranked" is a cop-out; if no scenario warrants reaching for X over Y, X is strictly dominated → eliminate. Caught under user pushback ("why not ranked?").
- [auto] S5 [pattern]: When a third-party tool emits its own bug-report URL, evaluate the supported-version envelope BEFORE filing upstream. Streamlit's "Please report this bug" on Python 3.11.0rc1 (release candidate from before 3.11.0 GA) deserves a venv upgrade test first. Filing on unsupported runtimes burns maintainer time on environment quirks.
- [auto] S5 [project]: Plans in `dsm-docs/plans/` may silently violate DSM_2.0.C Template 8 (no DSM tool audits plan-document structure). Before suggesting work on a sprint plan, verify it contains the Sprint Boundary Checklist + Phase Boundary Checklist + Branch Strategy + How to Resume sections. If missing, retro-fit before closing the sprint.
- [auto] S5 [ecosystem]: WSL `python` may resolve to a broken Windows pyenv shim (`/mnt/c/.../pyenv-win/shims/python` with `/bin/sh^M: bad interpreter`). Always use `.venv/bin/python` explicitly in scripts and docs; never bare `python` in WSL projects.
## Session 6 (2026-04-18)

- [auto] S6 [pattern]: Destructive-action permission denial is a prompt to pick a reversible alternative, not to give up. `rm -rf .venv` denied → `mv .venv .venv.old-rc1` succeeded and preserved a fallback. Reversible moves should be the default when the intent is to "replace" rather than "permanently delete."
- [auto] S6 [ecosystem]: WSL system Python (`/usr/bin/python3.11`) can be a release-candidate build (3.11.0rc1) that never auto-upgrades. Before treating a "fixed in 3.11.x" rumor as applicable, check the exact version with `python3.11 --version`, not just the major.minor.
- [auto] S6 [pattern]: A short-timeout boot test (`timeout 10s streamlit run ...`) is sufficient evidence for "does it crash at startup" when the failure mode is a race in the server-start sequence. The "You can now view your Streamlit app" banner is printed only after the crashing code path, so banner present = rc1 crash absent. Longer waits add no information for boot-phase asymmetries.
- [auto] S6 [pattern]: Conditional Gate 1 approval ("approve if X is true") is a useful user pattern. The agent should read/verify X first, report content, then proceed. Treating the conditional as unconditional approval risks acting on an assumption the user wants verified.
- [auto] S6 [pattern]: When DSM_2.0.C says "sprint branch merges back to session branch, session branch merges to main," a session wrap-up in the middle of a sprint should push the session's Sprint-1-scope work to main via the session branch, but leave sprint-scope work on the sprint branch. Conflating the two branch lives causes premature main merges.
- [auto] S6 [ecosystem]: When a spoke receives a playbook from another spoke (graph-explorer contribution playbook), integrate it into the plan doc as specific MUST/SHOULD items with explicit attribution ("per GE playbook"), not as general guidance. This makes the cross-spoke knowledge transfer auditable and keeps the lesson portable to other spokes.

## Session 7 (2026-04-18 / 2026-04-19)

- [auto] S7 [pattern]: Before asking the user a setup question (package manager, build tool, language version), probe repo state first: lockfile presence, pyproject build-system table, README install commands. Most "configuration" questions are derivable from the repo in <60 seconds. Caught at 11:16 when user pushed back on a package-manager question whose answer was `uv.lock` at repo root.
- [auto] S7 [pattern]: When a `/dsm-go` is re-invoked mid-session (e.g., next-day continuation), detect existing transcript + active session branch, and SKIP Step 5.5 (transcript archive) and Step 6 (transcript reset). Re-running them would destroy the live session history. Append a continuation thinking block instead.
- [auto] S7 [pattern]: For "organize then wrap-up" requests, split into two phases: Phase A commits + pushes the WIP work-in-progress so the user can review on GitHub before the wrap-up runs MEMORY/lessons updates. Phase B (`/dsm-wrap-up`) lands the meta artifacts in a separate commit. Cleaner GH history than a single combined commit, easier human review.
- [auto] S7 [pattern]: When the project plan declares a non-default branch model (e.g., Sprint 2's "session branches merge to sprint branch, not main"), `/dsm-wrap-up` Step 10's default "merge to main" is wrong. Honor the project-specific branch strategy from the sprint plan; document the deviation in the wrap-up summary so the next session sees it.
- [auto] S7 [project]: Stale README setup commands (e.g., `pip install -e .` after `uv.lock` lands) accumulate silently because no test exercises the docs. Flag at sprint close as a SHOULD cleanup, not in-flight; in-flight churn distracts from the deliverable. Documented in S7 wrap-up.

## Session 8 (2026-04-19 / 2026-04-21)

- [auto] S8 [infrastructure]: `validate-transcript-edit.sh` enforces `new_string` must START WITH `old_string` byte-for-byte, then appended content. When the anchor block is followed by trailing blank lines at EOF, the old_string must include those blanks AND the new_string must preserve them before appended content. Two failed Edit attempts resolved by capturing the exact tail (including trailing newlines) as old_string and prepending it verbatim to the new content.
- [auto] S8 [pattern]: `/dsm-light-wrap-up` cadence gate (BACKLOG-326) correctly hard-refuses multi-day branches without offering a bypass. When branch name date differs from today's date, route user to full `/dsm-wrap-up` immediately; do not attempt to salvage the light path. Observed on session-8/2026-04-19 refused on 2026-04-21.
- [auto] S8 [pattern]: `/dsm-go` Step 1.8 auto-`/dsm-align` writes the version check to `.claude/last-align-report.md` but does not surface version deltas in the Step 8 conversation report. A subsequent explicit `/dsm-align` re-run caught a 1.5.4 → 1.6.3 DSM version jump with 9 spoke actions that would otherwise have remained in the persistent file unread. Consider surfacing version deltas in `/dsm-go` Step 8 when `last-align-report.md` shows a version change.

## Session 9 (2026-04-21 / 2026-04-22)

- [auto] S9 [ecosystem]: When a user provides live UI data (subscription reset times, usage percentages) that conflicts with a local config file, trust the live data immediately. Stale config files are silent; the user's screen is authoritative. Observed: `claude-subscription.md` said "Friday 7:00 AM," user's Claude UI said "Thursday 21:00." Agent sourced from file without challenging. Update the file, do not repeat the stale value.
- [auto] S9 [ecosystem]: "No deferral, only reorder against pool topology" is the correct framing for resource-constrained sprint execution. When usage limits bite, classify each task by pool (all-models-weekly / sonnet-only / extra-usage), then reorder so reading-heavy and mechanical work runs in the pool with most headroom. No work is lost; only sequencing changes. Productively maps onto PMP procurement discipline.
- [auto] S9 [pattern]: Halt before executing mechanical work when the current model does not match the approved resource plan. "Start T2 on Sonnet" while on Opus 4.7 is a protocol violation even under user enthusiasm. Flag the mismatch, offer to commit pending work first, then stop for model switch. Observed: user said "start T2 now"; agent correctly halted, explained, committed S9 setup artifacts, then waited for `/model sonnet`.
- [auto] S9 [pattern]: Before writing a spike script, read the sprint plan's Experiment Gate section. The three-outcome classification (A/B/C), the target model, and the output artifact format are already specified there. Reading the Gate before writing saves reasoning time and produces a script that directly maps to plan outcomes. Observed: EXP-002 Gate in Sprint 2 plan specified PASS/FAIL-WITH-SIGNAL/FAIL-NO-SIGNAL; spike Sections 1-2 mapped exactly.
- [auto] S9 [ecosystem]: "Chain of reasoning" is an underspecified name for a provenance artifact because it implies linear sequence. When the actual structure is a directed graph of interdependencies (multiple research docs feeding one decision, one decision scoping multiple sprint phases), name it accordingly: DAG. The name shapes the format; "chain" leads to a list, "DAG" leads to a node registry + edge registry + Mermaid diagram. User caught this; agent adopted immediately.
- [auto] S10 [skill]: Skill silence is the answer. When a DSM skill is correctly invoked and returns without flagging concern X, do not add adjacent checks outside the skill's declared scope and fold their findings into the skill's report. The correct response to a clean `/dsm-align` run on a spoke is "aligned, no actions needed," not an invented action list built from off-skill checks. Extends DSM_0.2 §8.6 from "don't claim skill behavior from memory" to "don't supplement skill behavior from outside its scope." Observed: executed /dsm-align on a spoke, added an out-of-scope `diff -q` on `~/.claude/commands/*.md` (user-scope, not spoke-scope), promoted finding into the skill's report as "Command sync: Drifted: 2" + spoke-action recommendation. Cost: ~115 transcript lines across 4 user correction turns before empirical re-run falsified the framing. Filter to apply: is this check part of the skill's documented steps? If no, label the finding out-of-band or file a BL to extend the skill.
- [auto] S10 [skill]: BL implementation is Central-scoped; spoke-action annotations are downstream instructions, sometimes user-scope (not spoke-scope). `~/.claude/commands/` drift is once-per-machine-per-version, not per-spoke-session. Do not populate a spoke's alignment report with user-scope concerns even when they are real findings; they belong in a separate user-scope audit (ideally a distinct skill). User articulated the scope rule: "implementing BL items is responsibility and scope of only dsm central."
- [auto] S10 [pattern]: §8.7 BL-402 skip-by-match is enacted by absence, not by a meta-comment in the brief. When artifact demand matches the session baseline, omit the config-recommendation line entirely; do NOT write "no change" or "§8.7 skip condition applied" into the user-facing brief (the latter is itself ritualistic compliance under a different label). Log the skip reasoning in the transcript thinking block instead. Observed: T7 Phase 2 selection at Opus 4.7 baseline; correctly omitted the line in the Gate 1 brief, recorded the §8.7 check in the transcript.
- [auto] S10 [pattern]: When a sprint plan says "locked into X" (e.g., pyproject.toml), verify at Gate 2 whether X actually changes. If both candidates share the same dep, "locked into X" is satisfied vacuously and no edit is needed; flag the discovery as a scope narrowing rather than fabricating an edit to honor the plan's optimistic phrasing. Observed: Sprint 2 Phase 2 plan said "locked into pyproject.toml"; both bge-m3 and e5-base ride sentence-transformers>=5.4.1, so pyproject was untouched and Gate 2 narrowed from 3 to 2 artifacts.
- [auto] S10 [pattern]: BL-385 §8.2.1 counter-evidence must be genuine counter-evidence, not padding. An observation that points the same way as the recommendation (e.g., "tutorial-default trail favors X" when X is being rejected) is not counter-evidence; surfacing it as such dilutes the rule into mislabeled noise. Reject same-direction observations or relabel them as "confirming evidence". Observed: rejected the e5-base "tutorial-default" framing as CE for a bge-m3 recommendation because it argues against e5-base, not against bge-m3.
